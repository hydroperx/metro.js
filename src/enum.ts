/**
 * In-out variants for transitions.
 */
export type InOutVariant = "in" | "out" | "in-out";

/**
 * Alignment in a container such as `HGroup` and `VGroup`.
 */
export type Alignment =
  | "start"
  | "center"
  | "end"
  | "spaceBetween"
  | "stretch"
  | "left"
  | "right"
  | "top"
  | "bottom";
